---
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"

roles:
  - name: robertdebock.auditd
  - name: geerlingguy.nginx
  - name: geerlingguy.ntp
